<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cafeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launch_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launch_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.CafeApp">
        <activity
            android:name=".UserOrder"
            android:label="Store Orders"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".StoreOrders"
            android:label="Store Orders"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".OrderDetails"
            android:label="Order Details"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".OrderingCoffeeActivity"
            android:label="Coffee Order"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".OrderingDonutActivity"
            android:label="Donut Order"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>